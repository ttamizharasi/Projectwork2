services:
  - type: web
    name: fracturesense-ai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    rootDir: deployment
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
